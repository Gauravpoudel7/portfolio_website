version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/static/img:/app/app/static/img  # Persist images if needed
    environment:
      - ENV=production